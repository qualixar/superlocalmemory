# SuperLocalMemory - Web Dashboard Dependencies
# ============================================================================
# Install for the web dashboard only (no ML/search features).
#
# Download size: ~50MB | Install time: 1-2 minutes
# Install: pip3 install -r requirements-ui.txt
# Start:   python3 ~/.claude-memory/api_server.py
# ============================================================================

fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
python-multipart>=0.0.6,<1.0.0
