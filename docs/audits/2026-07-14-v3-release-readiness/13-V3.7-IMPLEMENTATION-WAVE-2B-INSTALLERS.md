# V3.7 implementation wave 2B — installer rebuild and activation boundary

Date: 2026-07-14
Release: `3.7.0` Memory Integrity
Status: installer implementation complete; hosted cross-platform proof and signed/notarized DMG proof remain open

## Outcome

Option B was approved. The supported npm, clone-based macOS/Linux, and clone-based
Windows paths now install application code into isolated environments. They do not
bootstrap a tool manager, mutate protected Python, configure an IDE, install hooks,
start a daemon, download models, or create a memory data root.

The activation pattern is deliberately two-stage:

1. `npm install -g superlocalmemory`, `uv tool install`, `pipx install`, or a clone
   installer establishes the executable environment only.
2. `slm setup` is the explicit consent boundary for profile selection, models,
   external IDE/plugin integration, hooks, and autostart.

npm lifecycle scripts do not prompt. npm does not provide a reliable interactive TTY
contract across terminals, CI, package managers, and global installs. The guided product
experience therefore begins in the explicit `slm setup` command.

This wave is not a V3.7 release approval. The repository is not a frozen release
candidate, the hosted Windows/macOS/Linux matrix has not run, the DMG channel is not
resolved, and other defect-ledger workstreams remain open.

## Supported channel contract

| Channel | Executable ownership | Setup behavior | Upgrade/uninstall behavior | Current proof |
|---|---|---|---|---|
| npm | Package-owned `.slm-venv`; wrapper invokes its exact interpreter | No implicit setup or external edits | npm owns code removal; durable data is never inspected | Packed tarball lifecycle passed on macOS |
| Python CLI | `uv tool` or `pipx` isolated application environment | User runs `slm setup` | Owning manager upgrades/removes code; data is preserved | Fresh wheel lifecycle passed through both managers on macOS |
| Repository clone, macOS/Linux | `scripts/install.sh` delegates to an existing uv or pipx | No prompts or external edits | install, upgrade, and uninstall select or require the owning manager | 20 black-box contract tests plus real uv/pipx artifact lifecycles |
| Repository clone, Windows | `scripts/install.ps1` delegates to an existing uv or pipx | No execution-policy, registry, PATH, or config mutation | automatic upgrade/uninstall detect ownership and fail closed on dual ownership | PowerShell parser/static contracts passed; runtime matrix pending hosted Windows CI |
| DMG | One frozen, version-matched local wheel | Isolated uv/pipx install; explicit setup; data-preserving uninstall | Complete payload manifest plus final DMG SHA-256/provenance sidecars | Rebuilt; signed/notarized release artifact proof remains required |

## Implemented safety boundaries

The npm installer validates Python 3.11–3.14, rejects a symlinked private-runtime
directory, installs the current npm artifact into that private environment, and checks
that npm and Python artifact versions agree. Its wrapper strips `PYTHONPATH` and invokes
only the package-owned interpreter.

The Unix and Windows clone installers accept a package, wheel, or repository source and
delegate only to an already-installed uv or pipx. They contain no remote-script pipe,
system pip, `sudo`, protected-Python bypass, shell-profile edit, service install, data-root
mutation, or implicit manager bootstrap. Ambiguous dual ownership fails closed.

Pre-uninstall is code-only. It neither reads memory locations nor prints destructive
data-deletion commands. Public install guidance now distinguishes the npm CLI, isolated
Python CLI, Python SDK virtual environment, and repository clone paths.

CLI metadata inspection is activation-free. Empty invocation, global help, command help,
and version output create no configuration, marker, database, daemon, hook, model, or data
directory.

## New defects discovered during artifact execution

| Defect | Evidence and correction | Current state |
|---|---|---|
| `SLM-P1-019` — obsolete DMG builder | Builder declared V2.7/MIT, test expected V2.1, copied removed files, advertised implicit IDE edits, and included destructive uninstall text | `fix_in_review`; rebuilt around a frozen wheel with strict candidate/release gates, but Apple signing/notarization proof is still pending |
| `SLM-P1-020` — metadata commands activate state | The first installed-wheel `slm --version` probe created `config.json` and `.version` | Fixed; subprocess and real artifact probes pass; hosted proof pending |
| `SLM-P1-021` — Windows auto lifecycle ignores ownership | Automatic upgrade/uninstall chose the first available manager | Fixed with owner detection and dual-owner fail-closed behavior; Windows runtime proof pending |
| `SLM-P0-012` — tenant signature cache aliases callable IDs | Full-suite order caused a tenant-aware cache hook to be treated as legacy because a destroyed callable's integer `id()` was reused | Fixed with weak callable keys; focused security group and complete suite pass |
| Existing `SLM-P0-005` — test isolation | Slow credential tests attempted to write a test secret to the real macOS Keychain and blocked for authorization | Tests now force keyring absence and use fixture-owned credential directories |
| `SLM-P0-013` — full-suite native teardown crash | LanceDB was imported while pytest collected a deselected native test, leaving its process-global background event loop alive at finalization | Native imports now occur only when the dedicated native tests execute; collection guard, default, slow, and native lanes exit normally |
| `SLM-P2-008` — rate-limit sweep uses an undefined key | Fatal-name lint and a new runtime regression proved expired agent cleanup raised `NameError` | Fixed to delete by the collected agent key; focused rate-limit suite passes |
| `SLM-P2-009` — worker finalizers emit unraisable broken-pipe warnings | The clean default run reported two `PytestUnraisableExceptionWarning` events from embedding and reranker finalizers | Registered as `reproduced`; lower priority than the closed crash mechanism but still required before stable release |

## Executed evidence

| Gate | Result |
|---|---|
| Installer/setup/metadata Python contracts | `97 passed, 8 skipped` (Windows runtime cases skipped on macOS) |
| npm lifecycle and interactive-profile Node contracts | `29 passed` |
| npm packed artifact | install, version identity, runtime identity, uninstall, and no-data checks passed |
| uv candidate-wheel lifecycle | install, version, upgrade, uninstall, and no-data checks passed |
| pipx candidate-wheel lifecycle | install, version, upgrade, uninstall, and no-data checks passed |
| Final current-tree package build | wheel and sdist built; both passed `twine check`; npm tarball packed; private runtime absent from all artifacts |
| Final current-tree npm lifecycle | packed tarball install, exact runtime `3.6.23`, package-owned environment, uninstall, and no-data checks passed |
| Final current-tree uv lifecycle | candidate wheel install, exact runtime `3.6.23`, upgrade, uninstall, and no-data checks passed |
| Final current-tree pipx lifecycle | candidate wheel install, exact runtime `3.6.23`, upgrade, uninstall, and no-data checks passed |
| Complete default suite | `6197 passed, 29 skipped, 318 deselected` in `159.67s`; clean exit code 0 |
| Complete slow lane, including release artifacts | `288 passed, 3 skipped, 6253 deselected` in `485.34s`; clean exit code 0 |
| Dedicated LanceDB native lane | `10 passed` in `0.64s`; clean exit code 0 |
| UI Node suite | `25 passed` |
| Tenant-isolation focused group | `21 passed` |
| Current installer/setup focused Python contracts | `38 passed, 8 skipped` (Windows runtime cases skipped on macOS) |
| Current npm lifecycle/profile contracts | `29 passed` |
| DMG frozen-artifact contracts | `9 passed`; Python compile and both shell syntax checks passed |
| Current-tree DMG candidate | Fresh `3.6.23` wheel built locally; unsigned DMG created, `hdiutil verify` passed, image mounted, complete payload/checksum/version validation passed, and install/uninstall launchers produced the expected uv dry-run commands |
| DMG distribution fail-closed gate | The unsigned candidate was rejected with `DMG is not signed with Developer ID` and exit code 2 under `--require-release-ready` |
| Fatal-name lint | Reduced from 15 findings to the two registered `SLM-P1-018` framework-adapter blockers |

The first slow-lane attempt was terminated after stack sampling proved it was blocked in
macOS Keychain authorization. The responsible test was isolated, corrected, and the slow
lane was restarted from zero. The green result above is from that complete rerun.

The first two post-change default-suite attempts reached 99% and then exited with code 139.
The second stack trace isolated a `LanceDBBackgroundEventLoop` that was created when pytest
imported the optional backend during test collection. LanceDB 0.30.2 does not expose a
public shutdown API for that module-global loop. The native test module therefore avoids
collection-time backend imports and runs in a dedicated process lane. This is isolation,
not suppression: the native lane remains a separate required CI job.

After subsequent discovery the ledger contains 48 unique defects: 15 P0, 21
P1, and 12 P2. This report records the Wave 2B evidence; later defect status is
authoritative only in `09-V3.7-DEFECT-LEDGER.yaml`.

## Hosted installer matrix

`.github/workflows/installer-matrix.yml` adds least-privilege artifact execution on
Ubuntu 22.04, macOS 14, and Windows latest. It builds the candidate wheel and npm tarball,
runs installer contracts, installs the artifacts, executes activation-free version probes,
asserts that no data root exists, and uninstalls the code. Actions are pinned to immutable
commit SHAs.

The workflow exists and parses locally. It has not run on GitHub in this working tree, so
its platform rows are proof-pending rather than verified.

The local package artifacts still identify as `3.6.23`. That is intentional before the
release candidate is frozen: synchronizing every package/version surface to `3.7.0` is
part of the release-provenance gate, not an ad hoc installer edit. These local artifact
passes prove behavior of the current tree; they are not registry or tag evidence.

## DMG decision and implementation

The approved choice was a full rebuild. `scripts/build-dmg.sh` now requires one
explicit local wheel, verifies its embedded version against `pyproject.toml`, and
packages only that artifact, the isolated installer, install/uninstall launchers,
and AGPL/notice files. `ARTIFACT-MANIFEST.json` inventories and hashes every
mounted payload file. Final DMG bytes receive a JSON provenance manifest and a
SHA-256 sidecar after optional signing/notarization hooks finish.

The hooks accept an existing Developer ID identity and `notarytool` keychain
profile; they accept no secrets. An unsigned build is labelled candidate-only.
`scripts/test-dmg.sh --require-release-ready` fails unless signing and Apple
notarization are both recorded and independently verified. Therefore the code
defect is fixed in review, while distribution remains blocked until a frozen
V3.7 wheel is signed, notarized, stapled, mounted, and passes the strict gate on
macOS. See `docs/install-macos-dmg.md` for the exact operator contract.

## Remaining release boundary

`SLM-P0-009` and `SLM-P1-020` are `proof_pending`, not `verified`. The macOS artifact
lifecycles are green, but Windows and hosted macOS/Linux rows must pass from the frozen
release candidate. `SLM-P1-021` and `SLM-P0-012` remain `fix_in_review` until their complete
row-specific security/platform proof is attached.

No package was published, no deployment was performed, no commit was created, and no
current LoCoMo score is claimed.
