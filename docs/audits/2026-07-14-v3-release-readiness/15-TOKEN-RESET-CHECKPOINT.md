# V3.7 token-reset checkpoint

Date: 2026-07-15

This is the authoritative resume point created before resetting the Codex
limit. All three repositories are committed and clean. Nothing was pushed,
published, tagged, signed, notarized, or deployed.

## Repository heads

| Repository | Commit | State |
|---|---|---|
| `superlocalmemory` | `1d181b00bfc1082d884bab8c4171fee4943d4132` | Clean; intentionally contains two bounded RED/WIP streams described below. |
| `superlocalmemory-website` | `eb2f5004e3ed990adc27c1bb487c2f91c06e96af` | Clean; strict check reports 0 errors, 0 warnings, and 0 hints; 34 pages build. |
| `superlocalmemory.wiki` | `d861cec4138181204ba2f923d2b33ce5885141b7` | Clean; truth/score-contract sync committed; not pushed. |

## Completed and locally verified at this checkpoint

- Canonical ingestion, scope-safe retrieval, authenticated mutation identity,
  daemon identity/readiness, lifecycle invariants, score-contract separation,
  reranker fallback truth, Fisher parity, semantic-cache wiring, recall feedback
  separation, mesh-state authentication, mDNS parsing, MCP profile fail-closed
  behavior, persisted int8 tiering, and resource cleanup have committed fixes.
- Exact Python/npm release-artifact gates, CycloneDX generation, release evidence,
  coordinated registry guards, package-license truth, and the now-removed DMG
  contract
  are committed. The root `uv.lock` is now source-controlled because release CI
  consumes it.
- The 17-client integration manifest is executable and honest: 12 clients have
  at least one reproducible local contract, five are static/manual only, and no
  external-host runtime is claimed. Independent rerun: 160 Python checks and
  32 Node plugin checks passed; evidence regenerated byte-for-byte.
- Held-out calibration-report tooling is committed. It computes Brier, ECE/MCE,
  reliability bins, and selective risk while keeping runtime status explicitly
  `uncalibrated`; 22 focused score/calibration tests passed.
- Starlette TestClient now uses the pinned `httpx2` developer backend, API startup
  uses lifespan rather than deprecated event hooks, and the plugin generator has
  an explicit `.mjs` module format.
- Website commercial offer, price, scope, exclusions, support, contact route,
  and explicit no-customer-case-study disclosure are committed. Website strict
  checks, source/rendered claim scans, and internal link checks pass.

## Intentionally incomplete committed streams

### P2-005 local privacy diagnostics

Commits: RED `e533bae`; WIP `1d181b0`.

The narrow lane currently has 6 passing and 2 failing tests. Resume by:

1. Fixing client-family normalization so unknown values are not matched by
   substrings such as `cli`.
2. Aligning the exact documentation contract.
3. Completing activation-event wiring.
4. Running the focused diagnostics, ingestion, engine, CLI, privacy, and setup
   regression lanes; commit a GREEN fix.

Estimated remaining implementation time from the assigned stream: about 20
minutes.

### P2-004 durable lineage and rebuild equivalence

Commit: RED `a92670d`.

The narrow tests intentionally fail at collection because
`M019_derivation_lineage` does not yet exist. Resume by:

1. Adding and registering M019 durable derivation-lineage storage.
2. Capturing exact spans for newly derived facts and recording paraphrases or
   legacy facts as unresolved instead of inferring or fabricating spans.
3. Extending the evidence bundle to summaries, scenes, profiles, edges, and
   index lineage.
4. Proving relational truth, FTS membership, and BM25 rebuild equivalence.
5. Making rollback restoration executable, not just exportable.
6. Keeping semantic/ANN equivalence explicitly unverified until a real rebuild
   proof exists.

Historical exact spans cannot be reconstructed when the original raw source is
absent. The release contract must state that limitation.

## Required next order

1. Finish P2-005 GREEN.
2. Finish the feasible P2-004 lineage/rebuild implementation and retain honest
   unresolved legacy provenance.
3. Run the complete default suite, slow lane, strict-warning/resource lane,
   benchmark-safe local lanes, build/wheel/sdist/npm exact-artifact lanes,
   SBOM/evidence verification, installer claim scans, and
   website/wiki checks.
4. Reconcile all 48 YAML ledger rows and the verification matrix only from
   executed proof. Do not mark external/platform evidence `verified`.
5. Commit the final release-candidate report. Do not bump/tag/publish stable
   `3.7.0` while Windows/Linux matrices, registry/tag
   parity, external-host integration execution, and other required external
   evidence are absent.

## Known external proof gates

- Current V3.7 LoCoMo performance is unknown; historical paper results must stay
  historical. LoCoMo is not a release gate, and no current score may be claimed.
- Windows/Linux and multi-version host matrices have not been executed here.
- The DMG distribution channel is removed by owner decision. npm, pip,
  `install.sh`, and `install.ps1` are the supported installer contract.
- PyPI/npm/GitHub tag and registry parity require the frozen release commit and
  explicit publication authority.
- External IDE/client runtimes are not proven by the local integration matrix.
